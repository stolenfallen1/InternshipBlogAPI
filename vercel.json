{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/posts",
            "dest": "/server.js"
        },
        {
            "src": "/get/:postId?",
            "dest": "/server.js"
        }
    ]
}